# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo

# Temporary
*.tmp
*.bak
*~

# Node / SPFx build output
node_modules/
package-lock.json
lib/
lib-commonjs/
lib-amd/
dist/
temp/
solution/
sharepoint/solution/
# Heft emits analysis logs, dependency audits and manifests here on every build.
# The CDN handoff bundle is assembled here too — all of it is build output.
release/
cdn-handoff/
*.sppkg
.heft/

# Local working files (don't commit personal graph data)
my-*.json
private-*.json
local-*.json

# Graph data exports — keep local-only by default to avoid committing real org data
enterprise-relationship-graph*.json
